using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Gates : MonoBehaviour
{
    /* Includes functions for interactable, non-item objects such as
     * doors, chests, or levers
     */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // Opens an interactable object if possible
    // Returns true if it can be opened, false if locked
    bool open()
    {

    }
}
